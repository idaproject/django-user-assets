============
Installation
============

At the command line::

    $ easy_install django-user-assets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-user-assets
    $ pip install django-user-assets
